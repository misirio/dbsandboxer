/**
 * Core API for database sandboxing.
 *
 * <p>This package contains the main interfaces and exceptions that define the database sandboxing
 * contract. The primary interface is {@link io.misir.dbsandboxer.core.api.SandboxDatabaseProvider}
 * which provides methods for preparing and rebuilding database sandboxes.
 *
 */
package io.misir.dbsandboxer.core.api;
